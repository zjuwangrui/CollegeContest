USB串口显示图片,不得缩小显示。调整串口发送负载来改变数据帧间隔，但是此间隔亦作为评测指标之一。
计算出当前图片的熵值（包括绝对熵值和相对熵值）并显示出来。
计算并打印出显示帧速，帧速不低于 1 帧/秒，精确到 0.1，也是评测指标之一，越快越好。
单片机通过熵值计算，完成一个闭眼检测算法，检测到闭眼时给出明确提示。闭眼检测允许一定的延时，即在闭眼一小段时间后进行提示，但此时间长度关系到系统性能指标（评测时会记录）。必须过滤眨眼动作。过滤眨眼后正常情况是长时间保持睁眼状态


灰度图像，串口发送负载改变数据帧间隔
LCD是3.2寸触摸屏，大小是否够。240*320分辨率
更换串口线。实测蓝线是TX线接PA2，电源线为红，地为黑，和老师给的图片不一样。
库函数调用不对似乎。
发送负载调整后帧率计算误差较大
记录测试数据
图片大小问题，不缩小显示。抠图会使图片大小固定为48*36
闭眼测试要求距离和矩形框大小适中，使得闭眼和睁眼的熵值差别较大，否则基本无法检测出来。要么就是要更新算法。感觉频谱特征或许也不错。睁眼的时候如果是瞳孔在照片中心，那么频谱差异会比较大。如果闭眼，大部分中心区域则是白色，频谱差异会比较小。可以尝试用频谱特征来做闭眼检测。